// functions
//to define functions, we have to use the key "def"

def simpleSum(num1:Double, num2: Double) : Double = {
  return num1+num2
}
simpleSum(1,2)
//if we use "return" key, we have to specify return type with ":" before type
//like  arguments(...) : Type = ...


def shortSum(num1:Double, num2:Double) = num1+num2
shortSum(1,2)

